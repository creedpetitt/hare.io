/**
 * Low-level HTTP fetch utilities for web tools.
 * - Performs GET with safe headers and redirect handling.
 * - Re-validates redirects against security policy.
 * - Returns raw HTML/text + response metadata.
 * - No extraction here; extraction lives in readability.ts.
 */
