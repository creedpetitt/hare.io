/**
 * Content extraction utilities.
 * - Converts HTML to readable text/markdown.
 * - Strips boilerplate (nav/ads), preserves headings and lists.
 * - Supports extractMode: "markdown" | "text".
 * - Enforces maxChars and truncation strategy.
 */
