/**
 * Playwright adapter.
 * - Initializes Playwright and connects to a browser instance.
 * - Handles page creation, navigation, screenshots, snapshots.
 * - Provides safe shutdown hooks.
 * - Isolated so Playwright remains optional in builds.
 */
